apiVersion: v1
kind: Service
metadata:
  name: sarraf-frontend-service
  labels:
    app: sarraf-frontend
spec:
  selector:
    app: sarraf-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
